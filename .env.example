# Copy this file to .env and fill in real values.
# NEVER commit the actual .env file.

# AWS credentials (scanning user - read only)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1

# Database (P3)
DATABASE_URL=sqlite:///./findings.db

# API (P3)
API_PORT=8000

# Dashboard (P3)
DASHBOARD_PORT=3000
