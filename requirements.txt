# P1 - Detection Engine
boto3==1.34.*
python-dotenv==1.0.*

# P2 - Scoring / Explainability
jsonschema==4.*
# scikit-learn added later if/when ML layer is built (Week 5+)
# shap added later only if ML layer is built

# P3 - Backend API
fastapi==0.111.*
uvicorn==0.30.*
sqlalchemy==2.0.*
pydantic==2.*

# Shared / dev
pytest==8.*
